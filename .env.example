# ============================================
# Vite Development Server Configuration
# ============================================

# Server port (default: 5173)
# VITE_PORT=5173

# Server host (default: ::)
# Use 'localhost' for local-only access, '::' for all interfaces
# VITE_HOST=::

# Automatically open browser when dev server starts (default: true)
# Set to 'false' to disable
# VITE_OPEN=true

# ============================================
# Build Configuration
# ============================================

# JavaScript build target (default: es2023)
# Options: es2015, es2016, es2017, es2018, es2019, es2020, es2021, es2022, es2023
# VITE_BUILD_TARGET=es2023

# Minification strategy (default: auto - esbuild in production, false in development)
# Options: 'false' (no minification), 'esbuild', 'terser'
# VITE_MINIFY=esbuild

# Sourcemap generation (default: inline in development, false in production)
# Options: 'true' (separate file), 'false' (no sourcemap), 'inline' (inline sourcemap)
# VITE_SOURCEMAP=inline

# CSS code splitting (default: true)
# Set to 'false' to disable CSS code splitting
# VITE_CSS_CODE_SPLIT=true

# ============================================
# Build Performance & Analysis
# ============================================

# Chunk size warning limit in KB (default: 1000)
# Warn if any chunk exceeds this size
# VITE_CHUNK_SIZE_WARNING_LIMIT=1000

# Report compressed size in build output (default: false)
# Set to 'true' to enable compressed size reporting
# VITE_REPORT_COMPRESSED_SIZE=false

# ============================================
# E2E Testing Configuration (Playwright)
# ============================================

# Base URL for E2E tests (default: http://localhost:5173)
# Override this if your app runs on a different port or URL
# E2E_BASE_URL=http://localhost:5173

# CI environment indicator (set automatically by CI systems)
# Set to 'true' to disable server reuse in Playwright webServer config
# CI=false

# ============================================
# Application Environment Variables
# ============================================
# Add your application-specific environment variables below
# Remember: Only variables prefixed with VITE_ are exposed to the client
#
# Example:
# VITE_API_BASE_URL=https://api.example.com
# VITE_APP_NAME=Screaming Architecture Starter
# VITE_APP_VERSION=1.0.0
