# Enforce package manager and engine versions from package.json
package-manager-strict=true
engine-strict=true

# Automatically install peer dependencies
auto-install-peers=true

# Use workspace protocol when in monorepo (pnpm-workspace.yaml exists)
prefer-workspace-packages=true

