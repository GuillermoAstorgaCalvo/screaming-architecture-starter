/**
 * useController re-export
 *
 * Provides react-hook-form's useController hook and supporting types through
 * the core forms adapter layer, preserving the abstraction boundary.
 */

export {
	type ControllerFieldState,
	type ControllerRenderProps,
	type FieldPath,
	type FieldValues,
	type Path,
	type PathValue,
	useController,
	type UseControllerProps,
	type UseControllerReturn,
} from 'react-hook-form';
