/**
 * Default values for Marquee component
 */
export const DEFAULT_SPEED = 50;
export const DEFAULT_DIRECTION = 'left';
export const DEFAULT_PAUSE_ON_HOVER = true;
export const DEFAULT_LOOP = true;

/**
 * Base CSS classes for the marquee container
 */
export const BASE_CLASSES =
	'relative flex w-full overflow-hidden whitespace-nowrap will-change-transform';
