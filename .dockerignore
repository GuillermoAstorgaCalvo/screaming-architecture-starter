# Dependencies
node_modules
.pnpm-store
.pnpm-debug.log

# Build outputs
dist
build
coverage
.vercel
.netlify
.vite

# Test outputs
playwright-report
test-results
.vitest
coverage
*.tsbuildinfo

# Environment files (keep .env.example, exclude sensitive files)
.env
.env.local
.env.*.local
.env.production
.env.development
.env.test

# IDE
.vscode
.idea
*.swp
*.swo
*~
.cursor

# OS
.DS_Store
Thumbs.db
desktop.ini

# Git
.git
.gitignore
.gitattributes
.gitmodules

# Docker
Dockerfile
Dockerfile.prod
docker-compose.yml
docker-compose.prod.yml
.dockerignore
.docker

# CI/CD
.github
.gitlab-ci.yml
.circleci
.travis.yml
Jenkinsfile

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Misc
.cache
.temp
.tmp
*.tmp
*.bak
*.swp
*.swo
*.pid
*.seed
*.pid.lock

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/

# Storybook build outputs
# Note: public/ is NOT excluded - it's needed for the build and runtime
storybook-static

# Temporary folders
tmp/
temp/

# Documentation (optional - include if you want docs in container)
# docs

# Local config files (exclude IDE/editor configs, but keep build configs)
.editorconfig
.prettierrc*
.eslintrc*

# Keep these (needed for build)
# package.json
# pnpm-lock.yaml
# pnpm-workspace.yaml
# tsconfig*.json (needed for TypeScript compilation)
# vite.config.* (needed for Vite build)
# tailwind.config.* (needed for Tailwind CSS)
# postcss.config.* (needed for PostCSS)
# eslint.config.* (needed for linting during build if used in CI)
# prettier.config.* (needed for formatting during build if used in CI)
# vitest.config.* (needed for tests)
# playwright.config.* (needed for E2E tests)

